import type { DashboardLayouts } from './types';

export const DEFAULT_LAYOUTS: DashboardLayouts = {
  // TODO: provide real default layouts per breakpoint once widgets are defined.
  lg: [{ i: 'widget-1', x: 0, y: 0, w: 4, h: 3 }],
};


